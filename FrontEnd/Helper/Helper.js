import React from "react";
import AsyncStorage from "@react-native-async-storage/async-storage";

const Helper_GetToken = async () => {
//   const [Token, setToken] = React.useState(null);

//   React.useEffect(() => {
//     (async () => {
//       try {
//         const value = await AsyncStorage.getItem("@storage_Key");
//         console.log("else value: ", await AsyncStorage.getItem("@storage_Key"));

//         if (value != null) {
//           setToken(value);
//         }
//       } catch (e) {
//         console.log(e);
//       }
//     })();
//   }, []);

  return 'Helper_GetToken';
};

export { Helper_GetToken };
